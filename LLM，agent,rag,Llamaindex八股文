一、
1.解决高并发



2.实现低延迟


3.RAG召回率recall（检索正确文档数和全部相关文档数的比值，也就是不漏掉有用资料的量化指标）
recall不漏掉信息，适合检索场景，优先确保素材齐全
跟精确度precision有区别，它是拿出来的文档都是有用的，减少无关文本混入上下文

RAG召回率低会导致答案缺少依据，AI幻觉，一本正经的胡说八道
//2. 影响召回率常见原因
1. 向量嵌入语义偏差，同义词、转述文本向量距离过大
2. 切块策略不合理：知识点被切分到不同块，单次检索只能命中部分片段
3. 只做向量检索，缺少关键词匹配，专有名词匹配失败
4. 阈值设置过高，稍微相似度偏低的文档直接被过滤
5. 没有查询改写、扩写，原问题表述和知识库文本措辞不一样
//3. 提升召回率的手段
1. 混合检索（多路召回）
向量检索 + BM25关键词检索，结果合并，弥补纯向量对实体、专有名词召回弱的缺点。
2. Query 增强
对原问题  做同义改写、生成子问题，一次性检索多条向量，扩大检索池。
3. 调整Chunk策略
块适当  放大，重叠切片；重要文档细粒度分段。
4. 降低相似度阈值，增加候选条数
多拿候选集，后面靠重排序(Rerank)剔除无关内容，牺牲部分精确率换取召回。
5. 父‑子检索
检索小块，取出完整父文档，避免上下文被拆分。
6. 优化Embedding模型，使用领域微调后的向量模型。
//4. 评测方式
1. 人工标注：给定问题，标出所有应该被检索到的文档，统计FN、TP。
2. 数据集评测：使用QA数据集，标准答案对应的段落是否落在召回列表。
3. 常用指标：Top‑k召回，即前k条结果里是否包含目标段落。
//5. 业务取舍
- 客服、专业知识库：优先 拉高召回，防止 关键信息缺失，再依靠 重排序提升精准度
- 普通对话：可以适度 降低召回，减少 冗余上下文，降低LLM输入token开销











